# Roadmap

- [x] Persistent username + personal PIN login stored in localStorage (auto-bypass on return visits)
- [x] `trips.owner_pin` / `owner_name` columns so trips belong to a user
- [x] Home route `/` = dashboard listing all of the user's trips with dates + total spend
- [x] Create New Trip from the dashboard (name, start/end dates)
- [x] `/trip/$tripId` detail route: expenses strictly filtered by trip id
- [x] "Back to trips" button in the tracker header; Log out in the dashboard header
